# 📋 Как посмотреть логи SATP

## 🎯 Варианты просмотра логов

### 1️⃣ В веб-интерфейсе (Event Log)

**Самый простой способ!**

```bash
cd web_demo
python3 app_real.py  # или app_real_with_logs.py
```

Откройте http://localhost:5000

- **Правая панель "Protocol Events"** - все события в реальном времени
- **Цветная индикация** - критичные события красным
- **Timestamps** - точное время каждого события

---

### 2️⃣ В терминале (при запуске app_real.py)

Все логи C++ программ выводятся прямо в терминал:

```bash
cd web_demo
python3 app_real.py

# Вы увидите:
[SERVER] [SATP-SERVER] ✓ Server started successfully
[SERVER] [SATP-SERVER] Listening on port: 5555
[CLIENT] [SATP-NET] Initializing UDP socket...
[CLIENT] [SATP] Step 1/3: Sending HELLO...
[SERVER] [SATP-SERVER] Processing: HELLO (seq: 0)
[CLIENT] ✓ HELLO_ACK received
# ... и т.д.
```

---

### 3️⃣ В файлах (при запуске app_real_with_logs.py)

```bash
cd web_demo
python3 app_real_with_logs.py
```

Логи сохраняются в `web_demo/logs/`:

- **`server_YYYYMMDD_HHMMSS.log`** - логи сервера
- **`client_YYYYMMDD_HHMMSS.log`** - логи клиента  
- **`combined_YYYYMMDD_HHMMSS.log`** - все вместе

**Просмотр в реальном времени:**

```bash
# В другом терминале
cd web_demo
tail -f logs/combined_*.log
```

---

### 4️⃣ Запуск сервера и клиента вручную (для отладки)

**Терминал 1 - Сервер:**
```bash
cd /home/rassulturtulov/Desktop/uni/research_methods/SATP
./satp_server 5555
```

**Терминал 2 - Клиент:**
```bash
cd /home/rassulturtulov/Desktop/uni/research_methods/SATP
./satp_client 127.0.0.1 5555
```

Вы увидите полные логи в каждом терминале!

---

## 🐛 Почему "Send Data" не работает?

**Причина:** Ваш C++ клиент (`satp_client`) - это **тестовая программа**, которая:
1. Подключается к серверу
2. Выполняет заранее запрограммированные тесты
3. **Автоматически отключается и завершается**

Кнопка "Send Data" в веб-интерфейсе **не может** отправить данные, потому что клиент уже завершился!

### Решения:

#### Вариант А: Запустить клиент заново

```bash
# Просто запустите клиент еще раз
cd /home/rassulturtulov/Desktop/uni/research_methods/SATP
./satp_client 127.0.0.1 5555
```

Клиент снова выполнит все тесты.

#### Вариант Б: Использовать веб-интерфейс для перезапуска

1. Нажмите "Disconnect"
2. Нажмите "Connect" снова
3. Клиент запустится заново и выполнит тесты

#### Вариант В: Модифицировать C++ клиент (для интерактивности)

Если хотите чтобы кнопка "Send Data" работала, нужно изменить C++ код:

```cpp
// В satp_client_test.cpp
int main() {
    // ... подключение ...
    
    // Вместо автоматических тестов, ждать команд:
    std::string command;
    while (std::getline(std::cin, command)) {
        if (command == "SEND") {
            // Отправить данные
        } else if (command == "QUIT") {
            break;
        }
    }
}
```

Но это требует изменения и перекомпиляции C++ кода.

---

## 💡 Рекомендация для презентации

**Лучший подход:**

1. Запустите `python3 app_real_with_logs.py`
2. Откройте http://localhost:5000
3. Нажмите **"Connect"**
4. **Не трогайте ничего!** Клиент автоматически:
   - Выполнит handshake
   - Отправит навигационные данные
   - Отправит критический алерт
   - Протестирует адаптацию батареи
   - Завершится

Все это **автоматически** отобразится в веб-интерфейсе!

**Для повторной демонстрации:**
- Нажмите "Disconnect"
- Нажмите "Connect" снова
- Клиент запустится заново

---

## 📊 Что показать на презентации

### Сценарий 1: Показать веб-интерфейс

1. Запустить `python3 app_real.py`
2. Открыть браузер http://localhost:5000
3. Нажать "Connect"
4. Показать Event Log справа - все события появляются
5. Показать анимацию пакетов
6. Объяснить что это реальные C++ программы

### Сценарий 2: Показать терминальные логи

1. Разделить экран на 3 части:
   - **Терминал 1:** `./satp_server 5555`
   - **Терминал 2:** Подождать...
   - **Терминал 3:** `./satp_client 127.0.0.1 5555`

2. Запустить клиент в Терминале 3

3. Показать как в Терминале 1 (сервер) появляются логи:
   ```
   [SATP-SERVER] Processing: HELLO
   [SATP-SERVER] ✓ Session created
   [SATP-SERVER] ✓ DATA from SMART_CANE_001
   [SATP-SERVER] Decrypted data: 51.160360,71.470270...
   ```

4. Объяснить что данные **реально зашифрованы** и **расшифровываются**

---

## ✅ Итог

**Для просмотра логов:**
- В браузере → Event Log (правая панель)
- В терминале → вывод `app_real.py`
- В файлах → `web_demo/logs/*.log`
- Отдельно → запустить `./satp_server` и `./satp_client` вручную

**"Send Data" не работает потому что:**
- Клиент - это тестовая программа
- Она выполняет тесты и завершается
- Для повторного теста нажмите Disconnect → Connect

**Для презентации:**
- Используйте автоматический режим (просто нажать Connect)
- Клиент сам выполнит все тесты
- Все отобразится в веб-интерфейсе

